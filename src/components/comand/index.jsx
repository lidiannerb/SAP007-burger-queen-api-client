export const Command = ({id, name, price, quantity }) => {
  return (
    <p>
      idProduct={id}
      nameProduct={name}
      priceProduct={price}
      quantityProduct={quantity}
    </p>
  );
};